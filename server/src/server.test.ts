// @ts-ignore
import { diesel } from '../../diesel-facade/dist/diesel-facade';

describe("diesel facade", () => {

	it("should be defined", () => {
		expect(diesel).toBeDefined();
	});

	it("should have a calc parser", () => {
		expect(diesel).toBeDefined();
	});

});
